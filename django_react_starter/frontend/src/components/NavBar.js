import {
    AppBar,
    Toolbar,
    Button
} from '@mui/material';

function NavBar() {
    return (
        <AppBar>
            <Toolbar>
                <Button>Help lol</Button>
            </Toolbar>
        </AppBar>
    )
}

export default NavBar;